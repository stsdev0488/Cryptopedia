import styled from 'styled-components/native';

import { Theme } from '@styles/theme';

export const DictionaryDetailStyles = {
  Desc: styled(Theme.Text)`
    line-height: 20px;
    margin-top: 20px;
  `,
  Wrapper: styled.View`
    padding: 10px;
  `,
};
