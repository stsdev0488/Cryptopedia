import { BarChart, LineChart } from 'react-native-svg-charts';
import styled from 'styled-components';

export const ChartStyles = {
  LineChart: styled(LineChart)`
    height: 200px;
  `,
  BarChart: styled(BarChart)`
    height: 200px;
  `,
};
