import { COLORS } from './colors';

export const MIXINS = {
  shadow: `
    shadow-color: ${COLORS.black};
    shadow-offset: 0 2px;
    shadow-opacity: 0.5;
    shadow-radius: 3px;
    elevation: 5;
  `,
};
